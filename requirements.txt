Flask>=3.0,<4.0
Werkzeug>=3.0,<4.0
cheroot>=10.0
cryptography>=42.0
smbus2>=0.4.3
# QR code for the Signal linking page. Pure Python, no dependencies of its
# own, so it installs offline on the Pi and inside the image build.
segno>=1.6,<2.0
